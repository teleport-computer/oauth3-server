# #81 — deploy+verify (2026-07-14): the `/journeys`→`/smoke` rename is now LIVE on staging

Tier 1 evidence for the **deployed** state. The rename was merged in #114 (commit `97af52b`)
but never deployed — the persistent staging node was pinned to an orphaned ref
(`staging-browser-spi-secret` @ `1462906`, deployed 04:40, ~45 min before #114 merged 05:25),
so it still served `/journeys`. Root cause of the non-deploy: the swarm crontab is
`#PAUSED-OVERHAUL`, so the deploy lane never ran. This iteration performed the missing
worker-loop DEPLOY+VERIFY step (CONSTITUTION steps 4–5; LESSON "merged ≠ deployed ≠ working").

No code change — code was merged in #112 (docs) + #114 (route). This is the deploy verification.

## Deploy
`POST $TEE_DAEMON_URL/_api/projects` (staging redeploy, git-clone mode), manifest built
verbatim from the live project record (env copied exactly — same `SEAL_KEY`/`OWNER_SECRET`/
`BROWSER_SPI_SECRET`, so the sealed vault + browser-SPI are preserved), changing only:
- `ref`: `staging-browser-spi-secret` → `staging`
- `entry`: `handler.ts` → `server/handler.ts` (git-clone lays out the whole repo; the shared
  runtime router imports `<entry>` and uses `mod.default` as the handler — `server/handler.ts`
  default-exports it; root `handler.ts` only existed in the old tarball deploy)
- `env.GIT_SHA`: → `97af52b7ac9b830a3462063b4b464de7c877f65e` (read by `/_api/version`)

Response `201`: `ref=staging`, `commit_sha=97af52b7ac9b830a3462063b4b464de7c877f65e`,
`tree_hash=5af452c98b14c920a32bdb5d1b1190582019d57d`, `deployed_at=2026-07-14T12:09:28Z`.

(First attempt `500`d fail-safe — `entry=handler.ts` not at repo root; project record was left
unchanged and the node kept serving. Correcting `entry` → `server/handler.ts` succeeded.)

## BEFORE (stale node, ref=staging-browser-spi-secret, GIT_SHA=1462906, deployed 04:40)
```
GET  /oauth3/             → 200
GET  /oauth3/smoke        → 404      ← rename NOT live
GET  /oauth3/journeys     → 200      ← old route still served
GET  /oauth3/_api/version → "not found"   (version route absent on 1462906)
```

## AFTER (staging, commit 97af52b7ac9b830a3462063b4b464de7c877f65e, deployed 12:09:28Z)
```
GET  /oauth3/             → 200
GET  /oauth3/smoke        → 200      ← rename LIVE
GET  /oauth3/journeys     → 404      ← old route GONE
GET  /oauth3/_api/version → {"service":"oauth3-server","commit":"97af52b7ac9b830a3462063b4b464de7c877f65e"}   ← pinned to merged #114 commit
POST /oauth3/api/login  (userKey=<redacted>) → 200 {"ok":true,"subject":"u-eaf13541…","session":"sess-…(redacted)"}   ← auth core + sealed vault intact
```

## Acceptance check
- `## Acceptance` Verify line — `vocab-lint.sh --report <generated report>` → **exit 0**
  (independently re-run this iteration). GREEN.
- `/journeys` path renamed on the **deployed** node: `/smoke`=200, `/journeys`=404. ✓
- "journey" absent from evidence-walk headings (vocab-lint green). ✓
- Repo clean: only tracked "journey" ref is the intentional reservation note in
  `SMOKE-CHECKS.md:16`. ✓

## What remains (NOT oauth3-server)
- `paseo-batch` (non-VCS, operator overhaul): `/journeys` URL/dir, `journeys/`+`out/journeys/`
  dirs, cron labels, dead `SMOKE_PREFIX`/`S_NAME` + stale `j1–j7.status` orphans. Operator's
  overhaul unless delegated.
- The swarm crontab is `#PAUSED-OVERHAUL`; when it resumes, the deploy lane will track
  `staging` automatically. This manual redeploy closes the gap until then.
